if command -v bunx >/dev/null 2>&1; then
    bunx --bun commitlint --edit "$1"
elif command -v bun >/dev/null 2>&1; then
    bun x commitlint --edit "$1"
else
    echo "Bun is not installed. Please install Bun to run commitlint."
    exit 1
fi